services:
  db:
    environment:
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-botler}
  bot:
    image: botler:dev
    build:
      dockerfile: ./docker/Dockerfile.dev
    environment:
      TYPEORM_PASSWORD: ${DATABASE_PASSWORD:-botler}
